# CRO Report Generator — Requirements

See original requirements document — full spec from which this MVP was built. Reference the plan at `~/.claude/plans/lazy-snacking-reef.md` for the MVP scope decisions.

The original document `CRO_Report_Generator_Requirements_v2 (1).md` contains:

- §2 — Wireframes and loading-screen step list
- §3 — Multi-page crawl strategy, page detection, ATC selectors, edge cases
- §5 — Page-type specific Baymard guideline coverage and payment detection patterns
- §6 — Scoring formula and category weights
- §7 — Report JSON schema
- §8 — Fynd advantages table (embedded verbatim in `lib/prompts/croAnalysis.ts`)
- §9 — Deferred pages (brand / collection / search)
